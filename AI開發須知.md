# AI 開發須知

這是給所有新加入專案的 AI 代理程式與工程師的強制入口文件。
在開始寫下第一行程式碼之前，你必須遵守以下規則，否則你的 PR 或修改將被拒絕。

## 必讀順序

在開始工作前，請依序確保你已經閱讀並理解以下文件(如缺少就自己依照目前專案創建)：
1. `README.md`
2. `docs/02_AI交接文件.md`
3. `docs/03_專案目前狀態.md`
4. `docs/04_技術選型與開發環境.md`
5. `docs/05_系統架構說明.md`
6. `docs/06_專案結構說明.md`
7. `docs/07_重要設計決策.md`
8. `docs/08_開發規範.md`
9. `docs/09_版本修改紀錄.md`

## 核心開發原則

1. **禁止直覺與破壞性重寫**：修改前先理解現況、需求、影響範圍與既有測試。不得因個人或 AI 偏好任意重寫或更換既有技術架構。
2. **延續決策**：優先延續已確認的設計；若需改變，先記錄決策並取得必要確認。
3. **保持相容**：保持既有功能與資料相容性，將重構拆成可驗證的小步驟。不得覆蓋或刪除不屬於本次任務的既有修改。
4. **版本編號與介面同步（強制）**：
   - 每次更新功能、修正錯誤或調整介面時，**必須在軟體介面頂部顯著位置同步顯示版本編號**（例如 `V3.03`、`V4.00`）。
   - **版本升級規則**：介面大改、重大架構擴充或跨模組功能升級主版本號（例如 `V3.00` -> `V4.00`）；功能微調、參數擴充或小幅修復升級次版本號（例如 `V3.01` -> `V3.02`）。
   - **版本修改紀錄對照**：每次版本變更的詳細項目、原因、影響、主要檔案與驗證成果，**必須同步強制記錄於 `docs/09_版本修改紀錄.md`**，以供精確對照。
5. **文件同步**：**如果你的修改影響了系統行為、架構或介面，你必須在同一個任務中同步更新對應的 `docs/` 文件。**
6. **語系與規範**：嚴格遵守繁體中文優先，Git 提交訊息必須全為繁體中文；嚴格遵守純黑紅白與 `ui-ux-pro-max` 規範，維持高級專業感。
7. **主動提供下一步建議**：每次任務完成後，應主動提出關於系統下一步更完善的功能建議與工程改善方案，協助專案持續精進。

## 文件同步對照與時機（強制）

文件不是任務結束後才補寫的摘要；每完成一個可獨立驗證的修改，就要在同一個工作回合同步更新，避免下一位接手者看到過期狀態：

| 變更內容 | 必須更新的文件 | 更新時機 |
| --- | --- | --- |
| 任一功能、介面、錯誤修正或測試結果 | `docs/02_AI交接文件.md`、`docs/09_版本修改紀錄.md` | 實作與驗證完成前 |
| 完成度、已知限制、驗證矩陣或版本狀態 | `docs/03_專案目前狀態.md` | 驗證結果確定後 |
| 技術套件、環境或建置方式 | `docs/04_技術選型與開發環境.md` | 技術選擇或版本變更同一任務 |
| 資料流、模組責任、事件／狀態機 | `docs/05_系統架構說明.md` | 架構或資料流變更同一任務 |
| 路徑、入口、檔案歸屬或目錄整理 | `docs/06_專案結構說明.md` | 檔案移動或新增前後 |
| 重大產品取捨、UI 決策或相容策略 | `docs/07_重要設計決策.md` | 決策確認後 |
| 命名、測試門檻、Git 或開發流程 | `docs/08_開發規範.md` | 規則或流程變更同一任務 |

版本紀錄至少要有日期、項目、原因、影響、主要檔案、驗證與已知限制；交接文件至少列出完成內容、驗證、未完成項目、風險與下一步。若只改程式，也不得省略 `02` 與 `09`。

## 專案目錄規則（強制）

1. **根目錄固定且精簡**：根目錄保留 `README.md`、`AI開發須知.md`、`docs/`、`ref/`、`share/`、`src/` 及必要建置／版本控制入口。依 2026-08-29 使用者指示，額外允許 `publish/`，但裡面只能放最終發布 ZIP，禁止放開發 EXE、設定或出圖資料。
2. **所有軟體實作集中 `src/`**：應用程式、Windows Agent、Node.js 程式、共用模組、圖片資產、測試、範例、驗證工具、建置產物與執行時資料，全部放在 `src/` 及其子目錄。
3. **文件與素材分離**：專案說明放 `docs/`，外部技術參考放 `ref/`，共用／交付素材放 `share/`；不得將程式碼或執行資料混入上述資料夾。
4. **新增檔案先確認歸屬**：任何新檔案建立前，先依前三項規則判斷目標路徑；若無法歸類，先更新本文件或向專案負責人確認，不得直接放在根目錄。

## 完成前強制提醒

1. **版本號嚴格遞增規範（每次修改必升級）**：只要程式碼、設定、UI、排版或功能有任何一筆變更，軟體版本號**強制必須逐次遞增**（例如 `V5.00` ➔ `V5.01` ➔ `V5.02` ➔ `V5.03`），絕不允許停留在舊版本號！
   - 每次修改必須同步更新以下所有位置：
     1. `docs/09_版本修改紀錄.md`（新增最新版本修改細項）
     2. `docs/02_AI交接文件.md` 與 `docs/03_專案目前狀態.md`
     3. Git Commit 訊息開頭強制加上最新版本號（例如 `feat: V5.02 訊息描述`、`fix: V5.02 訊息描述`）。
2. **自動 Commit 規範**：當你認為任務完成時，請自行進行「文件同步檢查」。每一次做完一個任務就請自動執行 `git add` 與 `git commit`（全繁體中文訊息，且**訊息開頭必須包含版本編號**），確保出錯可以快速返回與追溯歷程。**只需本機 commit，絕不主動 push 到遠端倉庫，除非使用者明確要求**。
3. **文件更新驗收**：請確保你已更新 `docs/09_版本修改紀錄.md`，並更新了 `docs/02_AI交接文件.md` 給下一位接手者。
4. 未完成文件更新與版本遞增，任務就不算完成！
